{
  "name": "enhanced-working-diffs",
  "displayName": "enhanced working diffs",
  "description": "",
  "version": "0.0.2",
  "publisher": "gosvig123",
  "engines": {
    "vscode": "1.96.2"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:enhanced-working-diffs.toggleInlineDiff",
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "enhanced-working-diffs.toggleInlineDiff",
        "title": "Toggle Inline Working Diff",
        "category": "Diff"
      }
    ],
    "keybindings": [
      {
        "command": "enhanced-working-diffs.toggleInlineDiff",
        "key": "ctrl+alt+m",
        "mac": "cmd+alt+m",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile",
    "test": "vscode-test",
    "publish": "vsce publish"
  },
  "devDependencies": {
    "@types/vscode": "1.96.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "typescript": "^5.7.3",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "@vscode/vsce": "^2.15.0"
  },
  "dependencies": {
    "simple-git": "^3.22.0"
  }
}
